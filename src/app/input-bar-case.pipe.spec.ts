import { InputBarCasePipe } from './input-bar-case.pipe';

describe('InputBarCasePipe', () => {
  it('create an instance', () => {
    const pipe = new InputBarCasePipe();
    expect(pipe).toBeTruthy();
  });
});
